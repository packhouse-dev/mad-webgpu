import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.aistudio.multiplayergame',
  appName: 'Multiplayer Game Prototype',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
